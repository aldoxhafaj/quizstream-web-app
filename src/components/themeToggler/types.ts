import type { IconButtonProps } from '@quizstream/components/iconButton/types';

export type ThemeTogglerProps = Pick<
  IconButtonProps,
  'size' | 'radius' | 'color' | 'className' | 'iconClassName'
>;
