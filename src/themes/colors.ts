/** These colors must be used only in unavoidable cases. You will be able
 * to use them through useTheme custom hook so you apply them based on theme.
 * Otherwise use tailwind classes.
 *
 * Rule ***
 * You should add here only colors that you use in critical cases. Also all the colors
 * that are here should be included in tailwind as well but not vice versa.
 */

export enum DarkColors {
  CHARCOAL = '#23262f',
  DUSK = '#35383f',
}

export enum LightColors {
  CHARCOAL = '#e1dfdf',
  DUSK = '#f3f0f0',
}
